@model ECommerceProject.Models.SubCategory

@{
    ViewData["Title"] = "Create SubCategory";
}

<h1>Create</h1>

<h4>SubCategory</h4>
<hr />

@* 1. Use the ViewData and Rajor Syntax for views *@
@* <div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <label asp-for="SubCategoryName" class="control-label"></label>
                <input asp-for="SubCategoryName" class="form-control" />
                <span asp-validation-for="SubCategoryName" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="CategoryId" class="control-label"></label>
                <select asp-for="CategoryId" class ="form-control" asp-items="ViewBag.CategoryId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success" />
            </div>
        </form>
    </div>
</div> *@

@* 2. Use the ViewData and Html for views *@
@* <div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="form-group mb-3">
                <label for="SubCategoryName" class="control-label">SubCategory Name</label>
                <input type="text" id="SubCategoryName" name="SubCategoryName" class="form-control" />
            </div>
            <div class="form-group mb-3">
                <label for="CategoryId" class="control-label">Category</label>
                <select id="CategoryId" name="CategoryId" class="form-control">
                    @Html.Raw(ViewData["CategoryOptions"])
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success" />
            </div>
        </form>
    </div>
</div> *@

@* 3. Use the ViewBag and Html for views *@
@* <div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="form-group mb-3">
                <label for="SubCategoryName" class="control-label">SubCategory Name</label>
                <input type="text" id="SubCategoryName" name="SubCategoryName" class="form-control" />
            </div>
            <div class="form-group mb-3">
                <label for="CategoryId" class="control-label">Category</label>
                <select id="CategoryId" name="CategoryId" class="form-control">
                    <!-- Loop through the ViewBag.CategoryList to generate options -->
                    @foreach (var category in (IEnumerable<SelectListItem>)ViewBag.CategoryList)
                    {
                        <option value="@category.Value">@category.Text</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success" />
            </div>
        </form>
    </div>
</div> *@

@* 4. Use the ViewBag for select option and Html for views *@
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="form-group mb-3">
                <label for="SubCategoryName" class="control-label">SubCategory Name</label>
                <input type="text" id="SubCategoryName" name="SubCategoryName" class="form-control" />
            </div>
            <div class="form-group mb-3">
                <label for="CategoryId" class="control-label">Category</label>
                <select id="CategoryId" name="CategoryId" class="form-control">
                    @Html.Raw(ViewBag.CategoryOptions)
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>





<div class="mt-3">
    <a class="btn btn-secondary" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
